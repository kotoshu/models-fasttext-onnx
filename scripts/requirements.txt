# Conversion-validation pins: exact toolchain the converters and
# registry/manifest validators are tested against.
onnx==1.22.0
onnxruntime==1.23.2
numpy==2.2.6
scipy==1.15.3
jsonschema==4.23.0
# unittest discovery imports the existing tests/test_onnx_model.py
# framework, which imports yaml at module level.
pyyaml==6.0.2
# Plan 16 ctx-LM trainer reads the Wikipedia parquet shard
# (scripts/train_ctx_lm.py); pyarrow provides the column reader.
pyarrow==25.0.0
