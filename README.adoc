// FastText ONNX Models for Kotoshu
//
// Pre-trained FastText word embedding models converted to ONNX format for use
// with Kotoshu spell checker.

image:https://img.shields.io/badge/ONNX-IR%20v11-blue[ONNX IR Version]
image:https://img.shields.io/badge/Models-27-green[Total Models]
image:https://img.shields.io/badge/Size-1.25%20GB-orange[Total Size]

== Overview

This repository contains FastText word embedding models that have been converted
from the original `.vec` format to ONNX format for efficient deployment and
inference.

These models provide semantic word embeddings for use in spell checking and
natural language processing applications.

=== Purpose

The FastText ONNX models serve as a semantic similarity backend for the Kotoshu
spell checker. They enable:

* Semantic word similarity calculations
* Context-aware spelling suggestions
* Cross-language semantic comparisons
* Efficient inference via ONNX Runtime

=== Features

* **Multi-language Support**: Models for 9 languages (de, en, es, fr, ja, ko, pt, ru, zh), each in three tiers (mini, fluency, full)
* **Optimized Format**: 37x compression from original FastText vectors for the full tier
* **ONNX Compatible**: Works with onnxruntime 1.23.2+
* **High Quality**: 300-dimensional embeddings in every tier; vocabulary 10K mini, 50K fluency (60K for de), 100K full
* **Easy Integration**: Drop-in compatibility with Kotoshu spell checker

== Models Available

[options="header"]
|===
|Language|Code|Vocab Size|Embedding Dim|Model Size|Source
|German|de|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.de.300]
|English|en|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.en.300]
|Spanish|es|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.es.300]
|French|fr|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.fr.300]
|Japanese|ja|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.ja.300]
|Korean|ko|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.ko.300]
|Portuguese|pt|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.pt.300]
|Russian|ru|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.ru.300]
|Chinese|zh|100,000|300D|114.44 MB|https://fasttext.cc/docs/en/crawl-vectors.html[FastText CC.zh.300]
|===

Sizes above are the `full` (fp32) tier. Every language also ships `fluency`
and `mini` int8 tiers — see the tier table under _Registry and
distribution_ and `registry.json`, the canonical catalog.

=== Compression Ratio

Original FastText `.vec` files: ~4.3 GB per language
ONNX format: 114.44 MB per language (full tier)

Compression: **37x smaller** with full semantic quality preserved.

== Model Specifications

=== Technical Details

* **ONNX IR Version**: 11
* **ONNX Opset Version**: 11
* **Compatibility**: onnxruntime 1.23.2+
* **Input**: `word_index` (int64, shape=[1])
* **Output**: `embedding` (float32, shape=[300])

=== Architecture

The ONNX models use a simple embedding lookup architecture:

1. **Constant Node**: Contains the embedding matrix (vocab_size x 300)
2. **Gather Node**: Retrieves the embedding for a given word index
3. **Squeeze Node**: Removes the batch dimension

.Model Architecture
[source,text]
----
word_index (int64[1]) → Gather → Squeeze → embedding (float32[300])
     ↓
Embeddings Matrix (Constant)
----

== Usage

=== Python with onnxruntime

[source,python]
----
import onnxruntime as ort
import numpy as np

# Load model
sess = ort.InferenceSession('models/en/fasttext.en.onnx')
input_name = sess.get_inputs()[0].name
output_name = sess.get_outputs()[0].name

# Get embedding for word index
word_index = 0  # Replace with actual word index from vocabulary
embedding = sess.run([output_name], {input_name: np.array([word_index], dtype=np.int64)})[0]

print(f"Embedding shape: {embedding.shape}")  # (300,)
print(f"Embedding: {embedding}")
----

=== Ruby with Kotoshu

[source,ruby]
----
require 'kotoshu'

# Get ONNX model (downloads or converts as needed)
cache = Kotoshu::Cache::ModelCache.new
onnx_path = cache.get_onnx_model('en')

# Use for semantic similarity
# (This feature is planned for future Kotoshu versions)
----

=== CLI

[source,bash]
----
# Download models using Kotoshu CLI
kotoshu cache download en

# Check cache status
kotoshu cache status

# List available languages
kotoshu cache list
----

== Model Metadata

Each model embeds its metadata as ONNX `metadata_props`
(`vocabulary_size`, `embedding_dimension`, `model_type`, and for tiers
also `quantization` and `tier`) and ships a sibling `vocab.json`
(word to row index). Every language directory carries a `tiers.json`;
the six original languages (de en es fr pt ru) also carry
`models/{lang}/metadata.json`:

[source,json]
----
{
  "version": "2026-02-08T04:25:29Z",
  "language": "en",
  "type": "onnx",
  "file": "fasttext.en.onnx",
  "checksum": "d9bcfaf25df624225efd1373641627bcefa178868cd4fd09b052022cc9e18671",
  "cached_at": "2026-02-08T04:25:30Z",
  "source_model": "cc.en.300.vec",
  "conversion_method": "fasttext_to_onnx.py",
  "opset_version": 11
}
----

== Verification

All models have been functionally verified:

[source,bash]
----
# Run verification tests
ruby scripts/verify_all_models.rb

# Test individual model
python3 scripts/test_onnx.py en

# Load-verify all 27 models (tiers included), writes docs/inventory.json
python3 scripts/inventory.py
----

== Registry and distribution

`registry.json` at the repo root is the canonical, versioned catalog of every
resource (Resource Spec v1, `kotoshu.resources/v1`): one entry per language per
tier, each with dimensions, vocabulary size, quantization, sha256, size,
license, and download URLs. It is generated by `scripts/build_registry.py` —
never hand-edited — and validated against `schemas/registry.schema.json` by
`scripts/validate_registry.py`. Resource ids (`kotoshu://models/{lang}/{tier}`)
are stable forever; content changes ship as a new release tag.

Every language ships in three tiers. Sizes are design targets; what actually
gates shipping is a measured eval against the `full` tier of the same language
(`top1_agreement`, `rank_corr`), recorded under `eval/reports/` and linked from
the registry via `eval_ref`.

[options="header"]
|===
|Tier|Size|Recipe|Role
|mini|~3 MB|top-10k vocab · full 300 dims · int8|slim/edge/wasm
|fluency|~15 MB|top-50k vocab · full 300 dims · int8|default tier
|full|~120 MB|current fp32 300d file, unchanged|maximum accuracy, opt-in
|===

Size comes out of the vocabulary and the byte width, never the
dimensions: measured evals showed fastText neighbour rankings do not
survive SVD dim reduction (rank_corr 0.87–0.91), while int8 with full
dims is near-lossless on the retained vocabulary (rank_corr 0.9999).
Vocabulary loss is reported by the coverage metric in each eval report.

An `int4` group-128 variant of the full tier (all 100k rows, 300 dims,
~16.2 MB per language vs 120 MB fp32; each row packed into signed
nibbles in three 128-element groups with one fp32 scale per group) has
been built and evaluated as an experiment (plan 68 B1) but is NOT
shipped: `scripts/build_int4.py` produces run-in-onnxruntime artifacts
whose round-trip is exact, yet only `ru` clears the fluency
`top1_agreement` gate (8/9 languages fail at 0.78–0.94 while
`rank_corr` passes everywhere). Evidence lives in
`eval/reports/int4.{lang}.json` with the cross-language verdicts,
group-scale overhead analysis and recommendation in
`eval/reports/int4.summary.json`; adopting any such tier — including the
kotoshu-rs `RowFormat` extension its per-group scales would need — is
owner-gated.

Binaries are distributed as GitHub Release assets; the URL contract is locked
for API v1:

....
https://github.com/kotoshu/models-fasttext-onnx/releases/download/{TAG}/{asset}
https://raw.githubusercontent.com/kotoshu/models-fasttext-onnx/{TAG-or-main}/registry.json
....

`{asset}` is the file basename, one-to-one with the repo layout. Tags and
releases are cut manually by the owner — the release workflow
(`.github/workflows/release.yml`) only reacts to a pushed `v*` tag and never
creates one. What lives in git versus on Releases, and the LFS freeze, is
recorded in link:docs/storage-decision.md[docs/storage-decision.md].

== Download

=== Direct Download

Models can be downloaded directly from the https://github.com/kotoshu/models-fasttext-onnx/releases[Releases] page.

=== Via Kotoshu (Recommended)

Kotoshu will automatically download and cache these models when needed:

[source,ruby]
----
# Ruby API
cache = Kotoshu::Cache::ModelCache.new
onnx_path = cache.get_onnx_model('de')
----

=== Manual Download with Git LFS

[source,bash]
----
# Clone repository with Git LFS
git clone https://github.com/kotoshu/models-fasttext-onnx.git
cd models-fasttext-onnx
git lfs pull

# Download specific model
wget https://github.com/kotoshu/models-fasttext-onnx/raw/main/models/en/fasttext.en.onnx
----

== Building from Source

If you want to convert the models yourself from FastText `.vec` files:

.Install Dependencies
[source,bash]
----
pip install onnx onnxruntime numpy
----

.Download and Convert
[source,bash]
----
# Download FastText vectors
wget https://dl.fbaipublicfiles.com/fasttext/vectors-crawl/cc.en.300.vec.gz
gunzip cc.en.300.vec.gz

# Convert to ONNX
python3 scripts/fasttext_to_onnx.py cc.en.300.vec models/en/fasttext.en.onnx --vocab-size 100000
----

== Scripts

The repository includes several utility scripts:

* `scripts/fasttext_to_onnx.py` - Convert FastText `.vec` files to ONNX format
* `scripts/test_onnx.py` - Test a single ONNX model
* `scripts/verify_all_models.rb` - Verify all models for functionality

=== Conversion Script

[source,bash]
----
# Convert a FastText model to ONNX
python3 scripts/fasttext_to_onnx.py <vec_file> <output_onnx> [--vocab-size N]

# Example
python3 scripts/fasttext_to_onnx.py cc.en.300.vec models/en/fasttext.en.onnx --vocab-size 100000
----

=== Verification Script

[source,bash]
----
# Test a specific language model
python3 scripts/test_onnx.py en

# Verify all models
ruby scripts/verify_all_models.rb
----

== License

These models are derived from the https://fasttext.cc/docs/en/crawl-vectors.html[FastText pretrained vectors],
which are licensed under the https://creativecommons.org/licenses/by-sa/3.0/[Creative Commons Attribution-Share-Alike License 3.0].

=== Attribution

These models are derived from the FastText pretrained word vectors,
which were trained on Common Crawl data by Facebook AI Research.

If you use these models, please cite the original FastText paper:

[source,bibtex]
----
@article{bojanowski-2017-enriching,
    title = "Enriching Word Vectors with Subword Information",
    author = "Bojanowski, Piotr and
      Grave, Edouard and
      Joulin, Armand and
      Mikolov, Tomas",
    journal = "Transactions of the Association for Computational Linguistics",
    volume = "5",
    year = "2017",
    pages = "135--146",
    doi = "10.1162/tacl_a_00051"
}
----

== References

* https://fasttext.cc/[FastText] - Facebook's library for efficient learning of word representations
* https://onnx.ai/[ONNX] - Open Neural Network Exchange
* https://github.com/microsoft/onnxruntime[ONNX Runtime] - Microsoft's cross-platform inference engine
* https://github.com/kotoshu/kotoshu[Kotoshu] - Spell checker library

== Contributing

See link:CONTRIBUTING.md[CONTRIBUTING.md] for guidelines on contributing to this repository.

Contributions are welcome for:

* Adding new language models
* Improving conversion scripts
* Bug fixes and optimizations
* Documentation improvements

== Support

* **Issues**: https://github.com/kotoshu/models-fasttext-onnx/issues[GitHub Issues]
* **Documentation**: https://github.com/kotoshu/kotoshu[Kotoshu Documentation]

== Release History

[options="header"]
|===
|Version|Date|Changes
|1.0.0|2026-02-08|Initial release with all 6 language models
|===
