# Impl: website announcement (final)

## Status

pending wave-2 verdicts. Deliverables: announcement on the website — the fleet results (wave-2, per-class columns), the open benchmark repo link, the SOTA-adoption roadmap summary, and the honest gaps. No claims without a frozen report behind them.

## Gate

Every number on the page traces to a committed eval/reports file.

## Consumers

The public.
